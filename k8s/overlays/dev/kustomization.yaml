apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bap-be-bot-dev

resources:
  - ../../base
  - namespace.yaml

secretGenerator:
  - envs:
      - .env
    name: secrets
    type: Opaque

images:
  - name: bap-be-bot
